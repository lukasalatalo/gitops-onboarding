apiVersion: v1
kind: Secret
metadata:
  name: langfuse-postgres-secret
type: Opaque
stringData:
  password: langfuse-password  # Change this to a secure password in production
  database-url: postgresql://langfuse:langfuse-password@langfuse-postgres:5432/langfuse

