apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-config
data:
  config.yaml: |
    # Models are managed via LiteLLM Dashboard/UI
    # Access at http://localhost:4000/ui (after port-forward)
    # Models stored in database (STORE_MODEL_IN_DB: true)
    model_list: []

